<div class="mt-4"></div>

  <%= simple_form_for [@restaurant, @review] do |f| %>

    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.button :submit, class: "btn btn-primary text-light mt-4" %>

  <% end %>

</div>
